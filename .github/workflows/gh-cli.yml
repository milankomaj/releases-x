name: gh-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         gh release create v0.1 ${{ github.action_path }}/file_2.txt

        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
