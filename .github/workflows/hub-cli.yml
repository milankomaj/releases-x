name: hub-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         hub release create -m "v0.1" "$(date +%Y%m%d%H%M%S)"

        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}