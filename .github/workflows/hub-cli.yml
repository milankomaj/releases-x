name: hub-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         hub release create -a file_1.txt -m 'release 1' v1

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}