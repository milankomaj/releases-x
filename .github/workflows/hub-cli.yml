name: hub-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         hub release create -a -t file_2.txt -m 'release 1'
        env:
          GITHUB_TOKEN: ${{secrets.ACCESS_TOKEN}}