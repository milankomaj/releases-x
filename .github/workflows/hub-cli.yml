name: hub-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         hub release create -a file_2.txt -m "v0.1" "2"
        env:
          GITHUB_TOKEN: ${{secrets.ACCESS_TOKEN}}