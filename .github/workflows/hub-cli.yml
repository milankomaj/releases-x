name: hub-cli
on:
 workflow_dispatch:


jobs:
  cli:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: cli
        run: |
         hub release edit rel2 -m '' -a ./file_2.txt
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}